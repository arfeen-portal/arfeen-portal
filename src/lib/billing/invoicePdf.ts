import { PDFDocument, StandardFonts, rgb } from 'pdf-lib';
export const dynamic = "force-dynamic";

export async function generateInvoicePdf(invoice: any, items: any[]) {
  // ... (poora code jo pehle bheja tha)
}
